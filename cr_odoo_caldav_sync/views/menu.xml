<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================
         Root Menu Visibility: Controlled by resConfigSettings
    ============================================================ -->
    <menuitem
        id="menu_caldav_root"
        name="CalDAV Sync"
        groups="base.group_user"
        web_icon="cr_odoo_caldav_sync,static/description/icon.png"
        active="0"
        sequence="100"
    />

    <!-- ============================================================
         CalDAV Accounts sub-menu
    ============================================================ -->
    <menuitem
        id="menu_caldav_accounts"
        name="CalDAV Accounts"
        parent="menu_caldav_root"
        action="action_caldav_account"
        sequence="10"
    />

</odoo>
