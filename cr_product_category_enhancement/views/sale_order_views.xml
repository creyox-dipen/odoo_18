<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_inherit_cr_product_category" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.cr.product_category</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <!-- Ensure context is passed on product_template_id in order_line tree -->
            <xpath expr="//field[@name='order_line']/list//field[@name='product_template_id']" position="attributes">
                <attribute name="context">{
                    'partner_id': parent.partner_id,
                    'quantity': product_uom_qty,
                    'pricelist': parent.pricelist_id,
                    'uom': product_uom,
                    'company_id': parent.company_id,
                    'default_list_price': price_unit,
                    'default_uom_id': product_uom,
                    'restrict_categ_evr': True,
                }</attribute>
            </xpath>
            <!-- Ensure context is passed on product_id in order_line tree -->
            <xpath expr="//field[@name='order_line']/list//field[@name='product_id']" position="attributes">
                <attribute name="context">{
                    'partner_id': parent.partner_id,
                    'quantity': product_uom_qty,
                    'pricelist': parent.pricelist_id,
                    'uom': product_uom,
                    'company_id': parent.company_id,
                    'default_lst_price': price_unit,
                    'default_uom_id': product_uom,
                    'restrict_categ_evr': True,
                }</attribute>
            </xpath>
            <!-- Ensure context is passed on product_id in order_line form -->
            <xpath expr="//field[@name='order_line']/form//field[@name='product_id']" position="attributes">
                <attribute name="context">{
                    'partner_id': parent.partner_id,
                    'quantity': product_uom_qty,
                    'pricelist': parent.pricelist_id,
                    'uom': product_uom,
                    'company_id': parent.company_id,
                    'default_uom_id': product_uom,
                    'restrict_categ_evr': True,
                }</attribute>
            </xpath>
        </field>
    </record>
</odoo>
