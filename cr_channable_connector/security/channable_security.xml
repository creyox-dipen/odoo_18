<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record model="ir.module.category" id="module_channable_category">
            <field name="name">Channable Connector</field>
            <field name="description">User access level for Channable Connector module</field>
            <field name="sequence">20</field>
        </record>

        <record id="group_channable_user_read" model="res.groups">
            <field name="name">User: Allow to view orders</field>
            <field name="category_id" ref="module_channable_category"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
            <field name="comment">These users can only view data; they cannot create, import, or cancel orders from Channable.</field>
        </record>

        <record id="group_channable_user_manage" model="res.groups">
            <field name="name">User: Allow to manage orders</field>
            <field name="category_id" ref="module_channable_category"/>
            <field name="implied_ids" eval="[(4, ref('group_channable_user_read'))]"/>
            <field name="comment">These users can import orders and mark them as shipped, but cannot create new markets, projects, or connections.</field>
        </record>

        <record id="group_channable_administrator" model="res.groups">
            <field name="name">Administrator</field>
            <field name="category_id" ref="module_channable_category"/>
            <field name="implied_ids" eval="[(4, ref('group_channable_user_manage'))]"/>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
            <field name="comment">These users can create new markets, connections, import orders, etc.</field>
        </record>
    </data>
</odoo>
